{
  "name": "ppdb-cahaya-ilmu",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:backend": "cd backend && npm install && npm run dev",
    "dev": "concurrently \"cd frontend && npm install && npm run dev\" \"cd backend && npm install && npm run dev\"",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
